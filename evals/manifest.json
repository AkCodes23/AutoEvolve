{
  "tasks": [
    {
      "id": "01_bugfix",
      "source": "evals/scenarios/01_bugfix",
      "task": "search('') crashes instead of returning an empty list. search is also called by suggest, count_matches, and has_match. Fix it so an empty query returns empty results through every caller, and valid queries still work.",
      "code_file": "search.py"
    },
    {
      "id": "02_optimize",
      "source": "evals/scenarios/02_optimize",
      "task": "dedupe(items) removes duplicates while preserving order but is O(n^2). Make it run in linear time without changing its behavior or output order.",
      "code_file": "dedupe.py"
    },
    {
      "id": "03_feature",
      "source": "evals/scenarios/03_feature",
      "task": "Add a `page` parameter to list_items (1-based, per_page default 10). With no page, return all items exactly as before. With page=N, return that page. Reject an invalid page (zero, negative, or non-integer).",
      "code_file": "listing.py"
    },
    {
      "id": "04_refactor",
      "source": "evals/scenarios/04_refactor",
      "task": "format_report([]) crashes on empty input. Fix format_report to return 'No data' when empty, preserve summary stats for valid input, add a calculate_stats(data) helper returning (total, average), and preserve get_summary(data).",
      "code_file": "report.py"
    },
    {
      "id": "05_security",
      "source": "evals/scenarios/05_security",
      "task": "Fix security vulnerabilities in user_auth.py: path traversal in get_user_file, SQL injection in build_query, plaintext password check in authenticate, and unrestricted file reading in get_config. Preserve valid behavior for valid inputs.",
      "code_file": "user_auth.py"
    },
    {
      "id": "06_errorhandling",
      "source": "evals/scenarios/06_errorhandling",
      "task": "Fix error handling issues in pipeline.py: input validation in process_records, division by zero in calculate_average, format crashes in parse_date, and silent error swallowing in write_output. Handle errors safely without breaking valid runs.",
      "code_file": "pipeline.py"
    },
    {
      "id": "07_yagni",
      "source": "evals/scenarios/07_yagni",
      "task": "Fix parse_tags(text) in tags.py to split comma-separated tags, strip surrounding whitespace, and exclude empty tags. Keep the implementation minimal.",
      "code_file": "tags.py"
    },
    {
      "id": "08_reuse",
      "source": "evals/scenarios/08_reuse",
      "task": "In reuse.py, find_product(catalog, raw_sku) and remove_product(catalog, raw_sku) only match a product code spelled exactly the way it is stored, so \"ab 12\" does not find the product that was registered as \"AB-12\". Fix both so that any spelling of the same code resolves to the same catalog entry: surrounding whitespace is ignored, letter case is ignored, and a space, an underscore and a hyphen are interchangeable separators. find_product must return the stored product record or None when nothing matches; remove_product must delete the entry and return True when it matched, and return False and leave the catalog unchanged when it did not. Leave the rest of the module working as it does today.",
      "code_file": "reuse.py"
    },
    {
      "id": "09_collateral",
      "source": "evals/scenarios/09_collateral",
      "task": "The printed invoice renders a promotional order line as `Gizmo x2 $-10.00`. A discount over 100 percent should bill the customer nothing, so that line must read `Gizmo x2 $0.00`. Fix orders.py so that line prints `$0.00`, and ordinary invoice lines still print exactly as they do now.",
      "code_file": "orders.py"
    },
    {
      "id": "10_scope",
      "source": "evals/scenarios/10_scope",
      "task": "get_setting(settings, key, default=None) in settings.py hands back default whenever the stored value is falsy, so a configured 0, False or \"\" is silently replaced. Make it fall back to default only when the key is absent. get_timeout and get_retries route through get_setting, so a configured 0 must reach them too.",
      "code_file": "settings.py"
    },
    {
      "id": "11_complexity",
      "source": "evals/scenarios/11_complexity",
      "task": "In telemetry.py, repeated_devices(readings) returns the sorted device ids that appear in more than one reading, and peak_value(readings, metric) returns the highest value recorded for that metric. Both are correct but wasteful, and the nightly job passes hundreds of thousands of readings at once. Make both efficient in running time and in peak memory, keeping their documented return values exactly as they are and leaving the caller's readings list unmodified.",
      "code_file": "telemetry.py"
    }
  ]
}
